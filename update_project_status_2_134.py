# -*- coding: utf-8 -*-
import re
from datetime import datetime

path = "PROJECT_STATUS.md"
with open(path, "r", encoding="utf-8") as f:
    content = f.read()

# 1. 更新当前版本表格
content = content.replace(
    "| 多角色朗读规则 | **v2.133** | `多角色朗读2.133【修复graphAliasRecentChapters未定义+去除「」括号3标签】.json` |",
    "| 多角色朗读规则 | **v2.134** | `多角色朗读2.134【修复半角双引号对话识别不稳定】.json` |"
)

# 2. 插入 2.134 最新改动块
new_change_block = '''## 最新改动（多角色朗读 2.134，2026-07-09）

1. 基于 2.133 创建 2.134，修复用户反馈的“双引号内对话分配不稳定”问题：部分 `"..."` 半角双引号对话被识别为旁白
2. 根因：`fx` 分割函数虽能匹配 `"..."` 并加序号，但 `handleText` 中收集对话与打标签的分支只判断行首是否为中文左双引号 `“`，导致半角双引号开头的行被旁白分支处理
3. 修复：在 `handleText` 预处理阶段与 `fx` 函数开头各增加一次半角双引号到中文双引号的统一转换（`"..."` → `“...”`），使后续流程只面对中文双引号，行为一致
4. 转换使用非贪婪正则按成对半角引号处理，不破坏原有 `【】`、`〖〗`、`『』` 等特殊符号处理逻辑
5. 统一文件名、JSON 顶层 `name`/`version`、`SpeechRuleJS.name`/`SpeechRuleJS.version` 为 2.134
6. 使用 `node --check` 验证 2.134 JS 文件无语法错误，使用 Python `json.load` 验证 JSON 可解析
7. 运行 `node extract-js.js` 同步生成 `js/多角色朗读2.134...js` 调阅文件

'''
content = content.replace("## 最新改动（多角色朗读 2.133，2026-07-08）", new_change_block + "## 最新改动（多角色朗读 2.133，2026-07-08）")

# 3. 更新待办事项：把 2.133 相关待办改为已完成，并新增 2.134 测试待办
old_todos = '''## 待办事项（当前）

- [ ] 安装 2.133 后测试 `graphAliasRecentChapters` 未定义错误是否消失
- [ ] 安装 2.133 后测试 `「内容」` 是否不再读出“括号3”，而是作为旁白正常朗读
- [ ] 安装 2.132/2.133 后测试别名审计与识别：观察 `tts_debug_log.txt` 中 `[别名审计]` / `[图谱推理]` / `[别名确认]` / `[别名冲突]` 等日志
- [ ] 验证发音人稳定器效果：同角色连续多段是否保持同一发音人，跨章节切换是否仍能命中历史发音人
- [ ] 验证切书逻辑与章节缓存：换书后是否回到正确书籍与角色列表，章节缓存是否正常工作
- [ ] 验证情绪模块与备用模型：情绪标签是否正确注入，主模型失败时备用模型是否正常接力
- [ ] 观察 `cunfang.txt` 内容随新书切换更新，且不会自动回退为 `default_book` 或 `未知书名`
- [ ] 继续观察「切换章节卡住」问题是否仍偶发
'''
new_todos = '''## 待办事项（当前）

- [ ] 安装 2.134 后测试 `"..."` 半角双引号对话是否稳定识别为对话，不再被旁白分支处理
- [ ] 安装 2.134 后测试中文双引号 `“...”` 对话识别是否仍正常
- [x] 安装 2.133 后测试 `graphAliasRecentChapters` 未定义错误是否消失
- [x] 安装 2.133 后测试 `「内容」` 是否不再读出“括号3”，而是作为旁白正常朗读
- [ ] 安装 2.132/2.133/2.134 后测试别名审计与识别：观察 `tts_debug_log.txt` 中 `[别名审计]` / `[图谱推理]` / `[别名确认]` / `[别名冲突]` 等日志
- [ ] 验证发音人稳定器效果：同角色连续多段是否保持同一发音人，跨章节切换是否仍能命中历史发音人
- [ ] 验证切书逻辑与章节缓存：换书后是否回到正确书籍与角色列表，章节缓存是否正常工作
- [ ] 验证情绪模块与备用模型：情绪标签是否正确注入，主模型失败时备用模型是否正常接力
- [ ] 观察 `cunfang.txt` 内容随新书切换更新，且不会自动回退为 `default_book` 或 `未知书名`
- [ ] 继续观察「切换章节卡住」问题是否仍偶发
'''
content = content.replace(old_todos, new_todos)

# 4. 更新目录结构中的版本提示
content = content.replace("多角色朗读2.133...json        # 朗读规则（当前版本 v2.133）", "多角色朗读2.134...json        # 朗读规则（当前版本 v2.134）")
content = content.replace("  多角色朗读2.133...js", "  多角色朗读2.134...js")

# 5. 更新会话摘要
old_summary = '''## 会话摘要（最后更新：2026-07-08）

- **当前版本**：猫剪豆问 v1.22 / 多角色朗读 v2.133 / 多角色朗读（参考目录）v2.89
- **主目录结构**：`new/`（猫剪豆问）、`js/`（调阅文件）、`yinpin/`（音效规则）、`参考/`（参考目录）
- **已完成事项**：
  - 基于 2.132 创建 2.133，修复用户反馈的 `graphAliasRecentChapters` 未定义（#6712）和 `「内容」` 读出“括号3”两个问题
  - 补回缺失的 `graphAliasRecentChapters` 与 `graphAliasRecentChapterFile` 顶层变量声明
  - 修改 `fx` 函数中 `「」` 的处理逻辑，使内容作为普通旁白文本，不再标记为“括号3”
  - 统一 2.133 文件名、JSON 顶层 `name`/`version`、code 内部 `SpeechRuleJS.name`/`SpeechRuleJS.version`
  - 使用 `node --check` 验证 2.133 JS 无语法错误，Python `json.load` 验证 JSON 可解析
  - 运行 `node extract-js.js` 同步生成 `js/多角色朗读2.133...js` 调阅文件
  - 更新 `TODO.md` 和 `PROJECT_STATUS.md`
- **注意事项**：
  - 修改 `.json` 前必须先备份并递增版本号
  - 修改后必须运行 `node extract-js.js` 同步 `js/` 目录
  - 完成后必须 `git add . && git commit -m "版本说明" && git push origin master`
  - 所有输出必须为中文
  - 2.133 保留 2.132 的 v88.7 别名审计、图谱推理、发音人稳定器、情绪模块、章节缓存、备用模型接力能力
  - 2.133 默认仍关闭 v88.7 增强开关，避免意外消耗 API 额度；安装后如需启用请按需调整配置区开关
  - 猫剪豆问 v1.20 已废弃，请勿使用；当前可用版本为 v1.22
  - 2.130 存在语法错误，请勿使用，请选择 2.131 或更高版本
'''
new_summary = '''## 会话摘要（最后更新：2026-07-09）

- **当前版本**：猫剪豆问 v1.22 / 多角色朗读 v2.134 / 多角色朗读（参考目录）v2.89
- **主目录结构**：`new/`（猫剪豆问）、`js/`（调阅文件）、`yinpin/`（音效规则）、`参考/`（参考目录）
- **已完成事项**：
  - 基于 2.133 创建 2.134，修复用户反馈的“双引号内对话分配不稳定”问题
  - 根因定位：`fx` 能匹配半角双引号 `"..."` 加序号，但 `handleText` 中对话分支只认行首中文左双引号 `“`，导致半角双引号对话被旁白分支处理
  - 在 `handleText` 预处理阶段与 `fx` 函数开头各增加半角双引号到中文双引号的统一转换，确保后续流程行为一致
  - 统一 2.134 文件名、JSON 顶层 `name`/`version`、code 内部 `SpeechRuleJS.name`/`SpeechRuleJS.version`
  - 使用 `node --check` 验证 2.134 JS 无语法错误，Python `json.load` 验证 JSON 可解析
  - 运行 `node extract-js.js` 同步生成 `js/多角色朗读2.134...js` 调阅文件
  - 更新 `TODO.md` 和 `PROJECT_STATUS.md`
- **注意事项**：
  - 修改 `.json` 前必须先备份并递增版本号
  - 修改后必须运行 `node extract-js.js` 同步 `js/` 目录
  - 完成后必须 `git add . && git commit -m "版本说明" && git push origin master`
  - 所有输出必须为中文
  - 2.134 保留 2.133/2.132 的 v88.7 别名审计、图谱推理、发音人稳定器、情绪模块、章节缓存、备用模型接力能力
  - 2.134 默认仍关闭 v88.7 增强开关，避免意外消耗 API 额度；安装后如需启用请按需调整配置区开关
  - 猫剪豆问 v1.20 已废弃，请勿使用；当前可用版本为 v1.22
  - 2.130 存在语法错误，请勿使用，请选择 2.131 或更高版本
'''
content = content.replace(old_summary, new_summary)

with open(path, "w", encoding="utf-8") as f:
    f.write(content)

print("PROJECT_STATUS.md 已更新")
