# TODO / 变更记录

## 变更历史

### v1.21（2026-07-03）
- **引擎**：`猫剪豆问（自然情绪版）v1.21.json`
  - 新增 `ENABLE_FINAL_RETRY = 1` 与 `FINAL_RETRY_DELAY_MS = 15000`
  - 7 次常规重试全部失败后，sleep 15 秒进行最终抢救重试（针对服务端 5xx / 101 握手失败后状态异常）
  - 新增 `PROTOCOL_EXCEPTION_FIRST_DELAY_MS = 2000`，协议/连接异常首次重试前至少等待 2 秒，最长退避延长至 20 秒
  - 完整记录 `ProtocolException` / `SocketException` 完整错误信息到日志
  - 新增 `SKIP_PURE_PUNCTUATION = 1`，过滤无引号包裹的纯标点段（避免分段残留单独句号被合成）
  - 保留 `""` 内的纯标点段（如乌鸦叫等拟声音效需要辨识）
  - **修复 `hasProtocolError` 变量作用域错误**：将其定义从循环内移到循环外，避免 `ReferenceError`
- **脚本**：`(脚本)猫剪豆问（自然情绪版）v1.21.json`
  - 同步 name/version/code 版本注释到 v1.21
- **工程**：
  - 生成 `js/猫剪豆问（自然情绪版）v1.21.js`

### v1.20（2026-07-03）
- **引擎**：`猫剪豆问（自然情绪版）v1.20.json`
  - 新增 `RETRY_APPEND_PUNCTUATION = 1` 开关，可重试错误时自动在文本末尾补 `。`
  - 复用现有 7 次重试、随机 aid/device_id、指数退避、空音频/过短音频判断机制
  - 避免重复追加句号，先用正则清理末尾已有标点
  - 日志中记录实际发送文本 `sendText`
- **脚本**：`(脚本)猫剪豆问（自然情绪版）v1.20.json`
  - 同步 name/version/code 到 v1.20
- **工程**：
  - 新增 `extract-js.js`，自动把 `.json` 中的 code/url 提取到 `js/` 目录
  - 生成 `js/猫剪豆问（自然情绪版）v1.20.js` 和对应脚本 js 文件

### v1.19 及更早
- 见历史版本文件与 git 提交记录

## 会话摘要

- **当前版本**：v1.21
- **主目录结构**：根目录存放引擎/脚本 `.json`，`js/` 存放提取后的 `.js`
- **已完成事项**：
  1. 备份并升级引擎与脚本到 v1.20
  2. 引入跳段修复：重试时补句号
  3. 升级引擎到 v1.21，修复 5xx/101 握手失败后重试无效问题
  4. 新增最终抢救重试、协议异常退避优化、纯标点段过滤
  5. 创建 `extract-js.js` 并同步生成 `js/` 文件
  6. 创建 `TODO.md`
- **注意事项**：
  - 当前版本为 v1.21
  - 如补句号策略导致语气异常，可把 `RETRY_APPEND_PUNCTUATION` 改为 `0` 关闭
  - 如长等待抢救重试影响体验，可把 `ENABLE_FINAL_RETRY` 改为 `0` 关闭
  - 实际生效文件是 `.json`，`js/` 仅用于阅读和 AI 调阅
