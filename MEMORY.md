# AI 记忆（多角色朗读项目）

> 本文件用于在项目会话之间保存关键上下文、用户偏好和未解决问题。
> 每次进入本项目时，AI 应先读取本文件，再读取 `PROJECT_STATUS.md` 和 `TODO.md`。

---

## 用户偏好

- **排查习惯**：用户使用的阅读 APK 没有内置日志功能，倾向于通过文件日志排查问题。
- **迭代方式**：偏好直接升级版本号（v1.x → v1.x+1）的方式发布修复，而不是原地修改。
- **关注重点**：当前最关注「猫剪豆问切换章节卡住不播放」的问题。
- **沟通风格**：用户希望 AI 能保留跨会话记忆，减少重复说明。

---

## 项目当前状态

| 组件 | 版本 | 文件 |
|------|------|------|
| 猫剪豆问（脚本+引擎） | **v1.14** | `new/(脚本)猫剪豆问（自然情绪版）v1.14.json`、`new/猫剪豆问（自然情绪版）v1.14.json` |
| 多角色朗读规则 | **v2.129** | `多角色朗读2.129【基于126保留情绪修复回退127别名校验】.json` |

---

## 待解决问题

1. **切换章节卡住**
   - 用户反馈换书后切换章节可能卡住不播放。
   - v1.13 尝试在 `handleBookSwitch()` 中清空旧书章节缓存，但使用了 `new java.io.File()`，违反 Rhino 安全规范，实际清理失败。
   - v1.14 已增加统一文件日志，等待用户提供 `tts_debug_log.txt` 中换书前后、卡住前后的日志。

2. **v1.13 缓存清理实现缺陷**
   - 根因：`java.io.File` 被 `RhinoClassShutter` 屏蔽，会抛安全异常。
   - 后续修复方向：改用章节缓存索引文件记录已缓存章节路径，换书时用 `java.deleteExternalFile()` 逐个删除。

---

## 最近变更摘要

### v1.14（2026-06-25）
- 统一脚本和引擎日志输出到 `/storage/emulated/0/Download/chajian/mingwuyan/tts_debug_log.txt`。
- 日志文件超过 500KB 自动清空。
- 方便用户在没有内置日志功能的阅读 APK 中排查问题。

### v1.13（2026-06-25）
- 尝试修复换书后旧书角色缓存/章节缓存残留。
- 但因 `java.io.File()` 问题，清理逻辑实际上未生效。

### v2.129（2026-06-23）
- 多角色朗读规则：保留情绪修复，回退 v2.127 的批量别名校验以恢复速度。

---

## 常用文件路径

- 脚本：`new/(脚本)猫剪豆问（自然情绪版）v1.14.json`
- 引擎：`new/猫剪豆问（自然情绪版）v1.14.json`
- JS 调阅：`js/new/(脚本)猫剪豆问（自然情绪版）v1.14_obj{0,1}.js`、`js/new/猫剪豆问（自然情绪版）v1.14.js`
- 日志文件：`/storage/emulated/0/Download/chajian/mingwuyan/tts_debug_log.txt`
- 音效规则：`yinpin/ttsrv-replaces4.json`

---

## 排查建议

- 切换章节卡住时，优先检查日志中的：
  - `【换书检测】`
  - `【换书】`
  - `【换书】清空旧书缓存失败`
  - `【章节标题】`
  - `【合成】`
- 如果日志显示缓存清理失败，确认是 v1.13 的 `java.io.File()` 问题，需要进一步修复。

---

## 下次会话接入建议

1. 先读取本文件了解上下文。
2. 再读取 `PROJECT_STATUS.md` 和 `TODO.md` 核对最新状态。
3. 询问用户是否有新的 `tts_debug_log.txt` 日志可提供。
4. 根据日志决定是否需要发布 v1.15 修复缓存清理问题。
